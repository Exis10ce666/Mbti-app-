# PersonaView image drop zone

Place your exported UI mocks or character illustrations here (e.g., `intp.png`, `enfj.png`, `welcome.png`).
The Flutter widgets reference these assets by filename; if a file is missing the app will fall back to a simple gradient placeholder.
